#pragma once

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>

// 以下是用到的标准库函数列表。需要移植请按此实现。
#define yaya_printf printf

#define max(a, b) ((a) > (b) ? (a) : (b))

// operator new
// operator delete
